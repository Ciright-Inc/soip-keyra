# SOIP — uses same Postgres as Keyra; schema isolated in `keyra_soip`
DATABASE_URL="postgresql://keyra:keyra@localhost:5432/keyra?schema=keyra_soip"

# Optional: Ciright Core sync (operational identity system of record)
# CIRIGHT_CORE_API_BASE_URL=""
# CIRIGHT_CORE_API_KEY=""

# Public URL for ecosystem launcher
# NEXT_PUBLIC_SOIP_URL="http://localhost:3060"
