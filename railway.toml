# Railway — SOIP.KEYRA.IE sovereign operational intelligence platform

[build]
builder = "nixpacks"

[deploy]
# `npm start` runs scripts/start-with-db.mjs: db push/seed (best-effort) then next start.
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
